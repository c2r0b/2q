type Location {
  pathname: String
  search: String
  origin: String
  hash: String
  host: String
  hostname: String
  href: String
  port: Int
  protocol: String
}

type Section {
  id: ID!
  title: String!
}

type Query {
  location: Location
  selMovie: String
  sections: [Section]
}

type CreateInfo {
  nodesCreated: Int
}

type CreateResults {
  info: CreateInfo
}

type Mutation {
  createSections: CreateResults
}

type SectionWhere {
  id: ID!
}

type SectionCreateInput {
  id: ID!
  title: String!
}